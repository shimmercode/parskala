<?php
/**
 * Reconstructed OTP auth (ionCube replacement). Logic lives in Vira OTP module.
 */
if ( ! defined( "ABSPATH" ) ) { exit; }
if ( ! class_exists( "PRK_Auth" ) ) {
	class PRK_Auth {
		public static function init() {}
	}
}
