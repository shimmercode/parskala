# نصب قالب ویرا

وردپرس پوشهٔ کل ریپو را به‌عنوان قالب قبول نمی‌کند (`style.css` گم شده).

فقط این فایل را در **ظاهر → افزودن → بارگذاری پوسته** آپلود کنید:

- [`dist/vira.zip`](https://github.com/shimmercode/parskala/raw/arena/01a05406-parskala/dist/vira.zip)

داخل زیپ باید پوشهٔ `vira/` با `style.css` و `functions.php` باشد.

اگر قبلاً `parskala-1.4.1-vira` را نصب کرده‌اید، آن را حذف کنید و `vira.zip` را بگذارید.
